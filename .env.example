# Supabase Configuration
# Copy this file to .env and fill in your actual credentials

# Supabase Project URL
SUPABASE_URL=https://wdpeoyugsxqnpwwtkqsl.supabase.co

# Supabase Anon Key (public key - safe to use)
# Get this from: Supabase Dashboard > Project Settings > API > anon public key
# This key respects Row Level Security (RLS) policies - safe for reading public data
SUPABASE_ANON_KEY=

# Security Notes:
# 1. Using ANON key is safer than service_role key
# 2. ANON key respects Row Level Security (RLS) policies
# 3. Good for read-only access to public/authorized data
# 4. No need to keep this secret (it's public)
